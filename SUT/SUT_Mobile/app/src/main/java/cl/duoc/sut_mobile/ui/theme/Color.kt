package cl.duoc.sut_mobile.ui.theme

import androidx.compose.ui.graphics.Color

// --- PALETA TEAL (Principal / Institucional) ---
// Equivalente a teal-900 de Tailwind (Para barras, textos fuertes)
val Teal900 = Color(0xFF134E4A)
// Equivalente a teal-700 (Para estados hover o variantes)
val Teal700 = Color(0xFF0F766E)
// Equivalente a teal-100 (Para fondos suaves de items seleccionados)
val Teal100 = Color(0xFFCCFBF1)

// --- PALETA ORANGE (Acción / Destacado) ---
// Equivalente a orange-500 (Botones principales, alertas)
val Orange500 = Color(0xFFF97316)
// Equivalente a orange-700 (Bordes, textos de acción)
val Orange700 = Color(0xFFC2410C)
// Equivalente a orange-50 (Fondos de tarjetas admin)
val Orange50 = Color(0xFFFFF7ED)

// --- NEUTROS (Fondos y Textos) ---
// Equivalente a slate-50 (Fondo general de la app, no blanco puro)
val Slate50 = Color(0xFFF8FAFC)
// Equivalente a slate-900 (Textos muy oscuros)
val Slate900 = Color(0xFF0F172A)
// Equivalente a slate-500 (Textos secundarios)
val Slate500 = Color(0xFF64748B)

val White = Color(0xFFFFFFFF)
val Black = Color(0xFF000000)
val ErrorRed = Color(0xFFEF4444) // red-500